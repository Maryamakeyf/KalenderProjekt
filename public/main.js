window.addEventListener("DOMContentLoaded", main);

function main() {
  renderDate();
  initClock();
  displayCurrentDay();
  initTodolist();
  loadTodoFromLocalStorage();
  renderTodoList();
  updateCalendar();
  // Init today view

  // Init todos list
  // Init calendar
}
