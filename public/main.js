window.addEventListener("DOMContentLoaded", main);

function main() {
  renderDate();
  initClock();
  displayCurrentDay();
  initTodolist();

  // Init today view

  // Init todos list
  // Init calendar
}
