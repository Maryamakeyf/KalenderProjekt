window.addEventListener("DOMContentLoaded", main);

function main() {
  renderDate();
  initClock();
  displayCurrentDay();
  updateCalendar();
  initTodolist();
  loadTodoFromLocalStorage();
  renderTodoList();

  // Init today view

  // Init todos list
  // Init calendar
}
