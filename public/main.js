window.addEventListener("DOMContentLoaded", main);

function main() {
  renderDate();
  initClock();
  displayCurrentDay();
  initTodolist();
  loadTodoFromLocalStorage();
  renderTodoList();

  // Init today view

  // Init todos list
  // Init calendar
}
